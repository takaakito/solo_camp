      <% if food.favorited_by?(current_user) %>
      <%= link_to food_food_favorites_path(food), method: :delete, remote: true,class:"btn btn-sm btn-danger"  do %>
      ブックマークから外す
      <% end %>
      <% else %>
      <%= link_to food_food_favorites_path(food), method: :post, remote: true,class:"btn btn-sm btn-info" do %>
      ブックマーク
      <% end %>
      <% end %>